[project]
name = "email-triage-env"
version = "0.1.0"
description = "Email triage environment"
requires-python = ">=3.9"

dependencies = [
    "fastapi",
    "uvicorn",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server:app"
